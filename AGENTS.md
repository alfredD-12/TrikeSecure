# Agent Instructions

When stuck on error fixing, do NOT use `git checkout`. Use alternative recovery methods like:
- `git stash` / `git stash pop`
- `git reset --soft HEAD~1`
- `git reflog` to recover from mistakes